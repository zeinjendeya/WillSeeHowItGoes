import Breadcrumb from "./ui/up/elements/breadcrumb";

export default function Page(){
  const breadcrumb = [{name: "Home", href: "/"}, {name: "Page", href: "/page"}];
  return (
      <main>
          {/* <Breadcrumb {...breadcrumb} /> */}
        
      </main>
  );
}